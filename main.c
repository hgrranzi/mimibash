#include "mimibash.h"

int	main(int argc, char **argv, char **envp)
{
	t_data	*head_data;

	//handle_signal();
	//copy_envp();
	head_data = init_data();
	while (1)
	{
		//fill_history();
		//parse_data();
		//exec_cmd(head_data, envp);
		//free_data();
		;
	}
	return (0);
}
